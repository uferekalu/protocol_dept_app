// Starter landing page. Replace with the "Currently Hosting" dashboard described in
// docs/PROTOCOL_APP_BRIEF.md Section 5 (screen 2) once the invitations API exists.
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-3xl font-semibold">Protocol Department</h1>
      <p className="max-w-md text-gray-600">
        Scaffold is running. Build the &quot;Currently Hosting&quot; dashboard here per
        the project brief.
      </p>
    </main>
  );
}
